package deckOfCards;

public class Test {
/*NOTES
 * What next? player class to create multiple players with different strategies?
 * Different strategies as classes?
 * Allow for multiple card decks?
 * Also, figure out different strategies?
 * Players have different amounts of money?
 * many tests to determine optimal strategy?
 * Allow for split hands?
 * Double down?
 * Insurance?
 */
	
	//2-7 including dealer
	
	/*
    System.out.println("Enter dealer name ");
    Scanner scanner = new Scanner(System.in);
    String dealerName = scanner.nextLine();
    
    System.out.println("Enter dealer wealth: ");
    Scanner scanner2 = new Scanner(System.in);
    String dealerWealth = scanner2.nextLine();
    
    System.out.println("Enter number of additional players between 1 and 6");
    Scanner scanner3 = new Scanner(System.in);
    String number = scanner3.nextLine(); //3
    
    int numPlayers = Integer.parseInt(number)+1; //4
    Player[] players = new Player[numPlayers];
	
    Player dealer = new Player(dealerName, Integer.parseInt(dealerWealth));
	Hand dealerHand = new Hand();
	dealer.setPlayerHand(dealerHand);
	players[0] = dealer;
	
	String currentPlayerName;
	Player currentPlayer;
	Hand currentPlayerHand;
	int currentPlayerWealth;
	
	for (int i = 1; i < numPlayers; i++) {
		System.out.println("Enter player " + i + " name ");
		scanner = new Scanner(System.in);
		currentPlayerName = scanner.nextLine();
		
		System.out.println("Enter player " + i + " wealth ");
	    Scanner scanner = new Scanner(System.in);
	    currentPlayerWealth = Integer.parseInt(scanner.nextLine());
	    
	    currentPlayer = new Player(currentPlayerName,currentPlayerWealth);
	    currentPlayerHand = new Hand();
	    currentPlayer.setPlayerHand(currentPlayerHand);
	    players[i] = currentPlayer;
	    scanner.close();
	    
    }
	for (int i = 1; i < numPlayers; ) {
		scanner.close();
	}
    scanner.close();
    scanner2.close();
    scanner3.close();*/

}
